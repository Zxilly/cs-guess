PANDASCORE_API_TOKEN=
BALLDONTLIE_API_TOKEN=

# Liquipedia requires an identifying User-Agent with project and contact details.
LIQUIPEDIA_USER_AGENT=CSGuess/0.1 (replace-with-project-url; replace-with-contact-email)

# Keep the HLTV fallback disabled unless its use has been explicitly reviewed.
ALLOW_HLTV_FALLBACK=false
